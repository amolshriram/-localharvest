import mongoose from 'mongoose';
const statuses = ['ORDER_PLACED','ORDER_CONFIRMED','MARKET_ASSIGNED','BUYER_ASSIGNED','SHOPPING_IN_PROGRESS','ITEMS_PURCHASED','AT_PACKING_POINT','PACKING','READY_FOR_PICKUP','DELIVERY_ASSIGNED','PICKED_UP','OUT_FOR_DELIVERY','DELIVERED','CANCELLED'];
const itemSchema = new mongoose.Schema({ productId: { type: mongoose.Schema.Types.ObjectId, ref: 'Product', required: true }, name: String, quantity: Number, unit: String, estimatedPrice: Number, actualQuantity: Number, actualPrice: Number, availability: { type: String, default: 'PENDING' } }, { _id: false });
const historySchema = new mongoose.Schema({ status: String, updatedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' }, note: String, timestamp: { type: Date, default: Date.now } }, { _id: false });
const schema = new mongoose.Schema({ customerId: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true, index: true }, items: [itemSchema], address: { type: String, required: true }, phone: String, latitude: Number, longitude: Number, marketId: { type: mongoose.Schema.Types.ObjectId, ref: 'Market' }, buyerId: { type: mongoose.Schema.Types.ObjectId, ref: 'User' }, packingPointId: { type: mongoose.Schema.Types.ObjectId, ref: 'PackingPoint' }, deliveryPartnerId: { type: mongoose.Schema.Types.ObjectId, ref: 'User' }, status: { type: String, enum: statuses, default: 'ORDER_PLACED', index: true }, paymentMethod: { type: String, default: 'COD' }, estimatedSubtotal: Number, actualSubtotal: Number, deliveryFee: { type: Number, default: 30 }, platformFee: { type: Number, default: 0 }, total: Number, statusHistory: [historySchema] }, { timestamps: true });
schema.index({ createdAt: -1 });
schema.statics.statuses = statuses;
export default mongoose.model('Order', schema);
