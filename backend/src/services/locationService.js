import Market from '../models/Market.js';
function distanceKm(lat1, lon1, lat2, lon2) { const toRad = value => value * Math.PI / 180; const dLat = toRad(lat2-lat1); const dLon = toRad(lon2-lon1); const a = Math.sin(dLat/2)**2 + Math.cos(toRad(lat1))*Math.cos(toRad(lat2))*Math.sin(dLon/2)**2; return 6371 * 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1-a)); }
export async function findNearestMarket(latitude, longitude) { const markets = await Market.find({ active: true }); if (!markets.length) throw Object.assign(new Error('No active market is available'), { statusCode: 409 }); return markets.map(market => ({ market, distance: distanceKm(latitude, longitude, market.latitude, market.longitude) })).sort((a,b) => a.distance-b.distance)[0].market; }
