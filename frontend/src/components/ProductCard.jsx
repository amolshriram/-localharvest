import { Plus } from 'lucide-react';
import { useCart } from '../context/CartContext';
export default function ProductCard({product}){const {add}=useCart();return <article className="product-card"><div className="product-art">{product.emoji||'🥕'}</div><div className="product-copy"><span className="eyebrow">{product.category}</span><h3>{product.name}</h3><p>Freshly sourced · {product.unit}</p><div className="product-footer"><strong>₹{product.estimatedPrice}<small> / {product.unit}</small></strong><button className="icon-button add-button" onClick={()=>add(product)} aria-label={`Add ${product.name}`}><Plus size={18}/></button></div></div></article>}
