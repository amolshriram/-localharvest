import { useState } from 'react'; import { Link,useNavigate } from 'react-router-dom'; import { ArrowRight,LockKeyhole,Mail } from 'lucide-react'; import { useAuth } from '../../context/AuthContext';
export default function Login(){const [form,setForm]=useState({email:'',password:''});const [error,setError]=useState('');const {login,loading}=useAuth();const navigate=useNavigate();async function submit(e){e.preventDefault();setError('');try{await login(form);navigate('/')}catch(err){setError(err.response?.data?.message||'Unable to sign in')}}return <div className="auth-page"><div className="auth-visual"><Link to="/" className="brand light"><span className="brand-mark">LH</span><span>local<span>harvest</span></span></Link><div className="auth-quote"><span className="kicker">Fresh, close to home</span><h1>Good food travels a shorter distance.</h1><p>Market-fresh produce, selected for your neighborhood and delivered with care.</p></div><div className="visual-note">Today’s market board <strong>● Open until 9:00 PM</strong></div></div><div className="auth-form-wrap"><form className="auth-form" onSubmit={submit}><span className="eyebrow">Welcome back</span><h2>Sign in to your market</h2><p className="form-intro">Pick up where you left off.</p>{error&&<div className="error-message">{error}</div>}<label>Email address<div className="input-icon"><Mail size={17}/><input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@example.com"/></div></label><label>Password<div className="input-icon"><LockKeyhole size={17}/><input type="password" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Your password"/></div></label><button className="primary-button full" disabled={loading}>{loading?'Signing in...':'Sign in'}<ArrowRight size={18}/></button><p className="switch-auth">New to localharvest? <Link to="/register">Create an account</Link></p><div className="demo-hint">Demo access: <b>admin@localharvest.demo</b> · Password123!</div></form></div></div>}
