import { useState } from 'react'; import { Link,useNavigate } from 'react-router-dom'; import { ArrowRight } from 'lucide-react'; import { useAuth } from '../../context/AuthContext';
export default function Register(){const [form,setForm]=useState({name:'',email:'',phone:'',password:'',address:''});const [error,setError]=useState('');const {register,loading}=useAuth();const navigate=useNavigate();async function submit(e){e.preventDefault();setError('');try{await register(form);navigate('/')}catch(err){setError(err.response?.data?.message||'Unable to create account')}}return <div className="auth-page"><div className="auth-visual register-visual"><Link to="/" className="brand light"><span className="brand-mark">LH</span><span>local<span>harvest</span></span></Link><div className="auth-quote"><span className="kicker">A better grocery ritual</span><h1>Meet your neighborhood market.</h1><p>Every order starts nearby, then moves through real hands before it reaches your door.</p></div></div><div className="auth-form-wrap"><form className="auth-form" onSubmit={submit}><span className="eyebrow">Create your account</span><h2>Start with the good stuff.</h2><p className="form-intro">A few details and your local market is ready.</p>{error&&<div className="error-message">{error}</div>}<label>Full name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Your name"/></label><label>Email address<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@example.com"/></label><div className="form-row"><label>Phone<input required value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})} placeholder="10 digit number"/></label><label>Password<input required type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="8+ characters"/></label></div><label>Delivery address<input required value={form.address} onChange={e=>setForm({...form,address:e.target.value})} placeholder="House, street, neighborhood"/></label><button className="primary-button full" disabled={loading}>{loading?'Creating...':'Create account'}<ArrowRight size={18}/></button><p className="switch-auth">Already a member? <Link to="/login">Sign in</Link></p></form></div></div>}
