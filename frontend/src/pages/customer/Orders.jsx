import { useEffect,useState } from 'react'; import { getOrders } from '../../services/orderService'; import OrderCard from '../../components/OrderCard';
export default function Orders(){const [orders,setOrders]=useState([]);useEffect(()=>{getOrders().then(r=>setOrders(r.data.orders)).catch(()=>{})},[]);return <div className="page narrow-page"><div className="page-title compact"><span className="eyebrow">Your history</span><h1>My orders</h1><p>Every basket, from market to doorstep.</p></div><div className="order-list">{orders.map(order=><OrderCard key={order._id} order={order}/>)}</div>{!orders.length&&<div className="empty-state"><h2>No orders yet</h2><p>Your first market basket is a few clicks away.</p></div>}</div>}
