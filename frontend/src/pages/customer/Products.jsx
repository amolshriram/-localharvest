import { useEffect, useState } from 'react'; import { Search, SlidersHorizontal } from 'lucide-react'; import { getProducts } from '../../services/productService'; import ProductCard from '../../components/ProductCard';
export default function Products() { const [products, setProducts] = useState([]); const [query, setQuery] = useState(''); const [category, setCategory] = useState('All'); useEffect(() => { getProducts({ search: query, category: category === 'All' ? undefined : category }).then(r => setProducts(r.data.products)).catch(() => { }) }, [query, category]); return <div className="page"><div className="page-title"><div><span className="eyebrow">The market today</span><h1>Shop fresh produce</h1><p>Picked locally. Priced honestly. Delivered with care.</p></div></div><div className="catalog-toolbar"><div className="catalog-search"><Search size={18} /><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search tomatoes, mangoes..." /></div><div className="filter-tabs">{['All', 'Vegetables', 'Fruits'].map(item => <button key={item} className={category === item ? 'selected' : ''} onClick={() => setCategory(item)}>{item}</button>)}</div><button className="filter-button"><SlidersHorizontal size={17} /> Filters</button></div><div className="product-grid large">{products.map(product => <ProductCard key={product._id} product={product} />)}</div>{!products.length && <div className="empty-state">No produce matched that search.</div>}</div> }
